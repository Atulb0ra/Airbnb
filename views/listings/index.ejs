<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Airbnb</title>
</head>
<body>
    <h3>All Listings</h3>
    <form method = "action" action="/listings/new">
        <button>Create New Listing</button>
    </form>
    <% for(let listing of allListings){ %>
        <li><a href = "/listings/<%= listing._id%>" style="color : blue" ><%= listing.title %></a></li>
    <% } %>
</body>
</html>